name: My First Workflow

on: push

jobs: 
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v6

      - name: List and read files
        run: |
         echo "My first GitHub Actions job"
         ls -ltra
         cat README.md

      - name: Generate ASCII
        run: cowsay -f dragon "I am the dragon" >> dragon.txt


      # - name: List files
      #   run: ls

      # - name: Read file
      #   run: cat README.md
